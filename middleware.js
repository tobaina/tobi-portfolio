export const config = { matcher: '/' };export default function middleware(request) { try { const ua = request.headers.get('user-agent') || ''; if (/LinkedInBot|facebookexternalhit|Twitterbot|Slackbot|WhatsApp|TelegramBot|Discordbot|redditbot|Embedly|Pinterest|SkypeUriPreview/i.test(ua)) { return Response.redirect(new URL('/share.html', request.url).toString(), 302); } } catch (e) {} }
